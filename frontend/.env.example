# Local development can leave this empty so Vite uses the /api proxy.
# On Render, set this to your backend service URL plus /api.
# Example: VITE_API_URL=https://assignopedia-backend.onrender.com/api
VITE_API_URL=
